@inherits LayoutComponentBase

<div class="d-flex flex-column flex-md-row min-vh-100">
    <!-- SIDOMENY -->
    <nav class="bg-dark text-white p-3 flex-shrink-0" style="width: 250px;">
        <h4 class="text-center mb-4">HB Bank</h4>
        <NavMenu/>
    </nav>

    <!-- HUVUDINNEHÅLL -->
    <div class="flex-grow-1 d-flex flex-column">
        <!-- TOPPBAR -->
        <header class="d-flex justify-content-between align-items-center bg-light shadow-sm p-3">
            <span class="fw-bold">Välkommen till HB Bank</span>
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank" class="text-decoration-none">
                About
            </a>
        </header>

        <!-- SIDINNEHÅLL -->
        <main class="flex-grow-1 bg-light p-4" style="overflow-y:auto;">
            @Body
        </main>
    </div>
</div>